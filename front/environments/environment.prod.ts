const protocol = 'http';
const host = 'e';
const domain = 'yfq.cn';
export const environment = {
  production: false,
  protocol: protocol,
  host: host,
  domain: domain,
  webHost: protocol + '://' + host + '.' + domain
};
