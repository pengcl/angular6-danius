const protocol = 'http';
const host = 's';
const domain = 'danius.cn';
export const environment = {
  production: false,
  protocol: protocol,
  host: host,
  domain: domain,
  webHost: protocol + '://' + host + '.' + domain
};
