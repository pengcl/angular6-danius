const protocol = 'http';
const host = 'pinor';
const domain = 'heyazl.net';
export const environment = {
  production: false,
  protocol: protocol,
  host: host,
  domain: domain,
  webHost: protocol + '://' + host + '.' + domain
};
